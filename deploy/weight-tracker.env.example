# Kopieer naar weight-tracker.env (dat bestand staat in .gitignore, nooit
# committen) - zelfde patroon als app.env. Zonder deze twee variabelen
# draait de gewicht-tracker zonder login.
WEIGHT_APP_USERNAME=
WEIGHT_APP_PASSWORD=
